[package]
name = "cortex-orchestration"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
# Workspace dependencies
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
anyhow.workspace = true
futures.workspace = true
async-trait.workspace = true
chrono.workspace = true
uuid.workspace = true
async-stream.workspace = true

# Local Cortex dependencies
cortex-core = { path = "../cortex-core" }
cortex-types = { path = "../cortex-types" }
cortex-agents = { path = "../cortex-agents" }
cortex-intelligence = { path = "../cortex-intelligence" }
# cortex-runtime = { path = "../cortex-runtime" }  # Removed to break circular dependency
cortex-coordination = { path = "../cortex-coordination" }
cortex-consensus = { path = "../cortex-consensus" }

# External dependencies
tokio-stream = { version = "0.1.17", features = ["sync"] }
crossbeam-channel = "0.5"
rand = "0.9.2"

[dev-dependencies]
tokio-test = "0.4.4"
tempfile.workspace = true
